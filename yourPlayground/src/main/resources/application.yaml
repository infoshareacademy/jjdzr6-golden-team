spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3307/example
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      platform: mysql
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
